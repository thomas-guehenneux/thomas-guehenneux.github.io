---
import type { ImageMetadata } from 'astro'
import { Image } from 'astro:assets'

interface Props {
  src: ImageMetadata
  alt: string
  lang: 'en' | 'ja'
}

const { src, alt } = Astro.props
---

<Image {src} alt={alt} densities={[1, 2, 3]} loading="eager" decoding="auto" quality="high" />
