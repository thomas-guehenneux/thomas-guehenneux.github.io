---
import type { ImageMetadata } from 'astro'
import { Picture } from 'astro:assets'

interface Props {
  src: ImageMetadata
  alt: string
  lang: 'en' | 'ja'
}

const { src, alt } = Astro.props
---

<Picture
  {src}
  alt={alt}
  widths={[750, 860, src.width]}
  loading="eager"
  decoding="auto"
  quality="high"
  class="size-full object-cover"
  pictureAttributes={{
    class: 'size-full',
  }}
/>
